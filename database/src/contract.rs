use crate::prelude::AuthPayload;

/// query the contract and authenticate the auth payload
pub fn authenticate(_auth_payload: &AuthPayload) -> bool {
    // write code to authenticate the contract

    true
}

